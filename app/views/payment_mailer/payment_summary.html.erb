<!DOCTYPE html>
<html>
<head>
  <meta content="text/html; charset=UTF-8" http-equiv="Content-Type" />
  <style>
    body { font-family: sans-serif; color: #292929; background: #f9f9fe; margin: 0; padding: 24px; }
    .container { max-width: 560px; margin: 0 auto; background: #fff; border-radius: 8px; padding: 24px; border: 1px solid #eef0ff; }
    .logo { font-size: 18px; font-weight: bold; color: #13139c; margin-bottom: 16px; }
    .greeting { margin-bottom: 20px; }
    table.summary { width: 100%; border-collapse: collapse; margin: 16px 0; font-size: 14px; }
    table.summary th, table.summary td { padding: 8px 12px; text-align: left; border-bottom: 1px solid #eef0ff; }
    table.summary th { color: #6b7280; font-weight: 600; }
    table.summary .amount { text-align: right; }
    .note { margin: 20px 0; padding: 12px; background: #f9f9fe; border-radius: 6px; font-size: 13px; color: #292929; }
    .footer { margin-top: 24px; padding-top: 16px; border-top: 1px solid #eef0ff; font-size: 12px; color: #6b7280; }
    .accent { color: #ffc700; }
  </style>
</head>
<body>
  <div class="container">
    <div class="logo">ODT Training</div>
    <div class="greeting">
      <p>Hello <%= @customer_name %>,</p>
      <% if @body_message.present? %>
        <p><%= simple_format(@body_message) %></p>
      <% end %>
      <p>Please find below your payment summary for the training class.</p>
    </div>

    <table class="summary">
      <tr><th>Class</th><td><%= @class_title %></td></tr>
      <tr><th>Date</th><td><%= @class_date %></td></tr>
      <tr><th>Customer</th><td><%= @customer_name %></td></tr>
      <tr><th>Total Paid</th><td class="amount"><strong>฿<%= number_with_delimiter(@total_paid) %></strong></td></tr>
    </table>

    <p><strong>Pricing breakdown</strong></p>
    <table class="summary">
      <tr><th>Base</th><td class="amount">฿<%= number_with_delimiter(@base) %></td></tr>
      <tr><th>Discount</th><td class="amount">-฿<%= number_with_delimiter(@discount) %></td></tr>
      <tr><th>Before VAT</th><td class="amount">฿<%= number_with_delimiter(@before_vat) %></td></tr>
      <tr><th>VAT 7%</th><td class="amount">฿<%= number_with_delimiter(@vat) %></td></tr>
      <tr><th>Total (incl. VAT)</th><td class="amount"><strong>฿<%= number_with_delimiter(@total_paid) %></strong></td></tr>
    </table>

    <div class="note">
      Payment Summary PDF is attached to this email.
    </div>

    <div class="footer">
      For any questions, please contact <a href="mailto:ODTTraining@odds.team">ODTTraining@odds.team</a>.
    </div>
  </div>
</body>
</html>
