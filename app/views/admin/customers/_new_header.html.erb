<header class="customer-edit-header" aria-label="Create customer header">
  <div class="customer-edit-header__main">
    <div class="customer-edit-header__info">
      <h1 class="customer-edit-header__title">Create new Customer</h1>
      <p class="customer-edit-header__sub text-muted">กรอกข้อมูลลูกค้าเพื่อใช้ลงทะเบียนและออกเอกสาร</p>
    </div>
    <div class="customer-edit-header__actions">
      <%= link_to admin_customers_path, class: "odt-btn odt-btn--ghost odt-btn--sm" do %>
        <i class="bi bi-arrow-left odt-btn__icon" aria-hidden="true"></i><span class="odt-btn__label">Back</span>
      <% end %>
      <button type="submit" form="customer_edit_form" class="odt-btn odt-btn--primary odt-btn--sm">
        <i class="bi bi-plus-circle odt-btn__icon" aria-hidden="true"></i><span class="odt-btn__label">Create</span>
      </button>
    </div>
  </div>
</header>
