ลำดับ,ชื่อ-นามสกุล,Email,Phone,บริษัท,ประเภท,ช่องทางที่มา,สถานะชำระเงิน,สถานะเอกสาร,Quotation No.,Invoice No.,Receipt No.,จำนวนคลาสที่เคยเรียนแล้ว,Notes
<% @attendees.each_with_index do |attendee, index| %>
<%= index + 1 %>,<%= attendee.name %>,<%= attendee.email %>,<%= attendee.phone || "" %>,<%= (attendee.participant_type == "Corp" ? (attendee.company || "") : "") %>,<%= attendee.participant_type || "Indi" %>,<%= attendee.seats || 1 %>,<%= attendee.source_channel || "" %>,<%= attendee.payment_status || "Pending" %>,<%= attendee.document_status || "" %>,<%= attendee.quotation_no || "" %>,<%= attendee.invoice_no || "" %>,<%= attendee.receipt_no || "" %>,<%= attendee.total_classes || 0 %>,"<%= (attendee.notes || "").to_s.gsub('"', '""') %>"
<% end %>
