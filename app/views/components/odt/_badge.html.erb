<%
  v = (local_assigns[:status].presence || variant).to_s
  css = ["odt-badge", "odt-badge--#{v}", "odt-badge--#{size}"]
  css << "odt-badge--pill" if local_assigns[:pill] != false
%>
<span class="<%= css.join(' ') %>"><%= text %></span>
