<div class="odt-table-empty" role="status">
  <% if icon.present? %>
    <div><i class="bi bi-<%= icon %>"></i></div>
  <% else %>
    <div><i class="bi bi-inbox"></i></div>
  <% end %>
  <p class="mb-0"><%= message %></p>
</div>
